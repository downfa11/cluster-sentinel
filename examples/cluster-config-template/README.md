# cluster-config-template

This directory is safe to publish. It contains fake example files that show the expected private `cluster-config` shape without exposing production data.

Never place real tokens, users, Tailnet names, Supabase URLs, internal domains, or production Helm values here.
